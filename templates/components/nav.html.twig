<nav class="navbar navbar-expand-lg bg">
	<div class="container-fluid">
		<a class="navbar-brand SiteTitleNav" href="#">Match2Love
			{# <img class="nav-img" src="../../public/uploads/images/coeur_bleu.jpg" alt="HeartLogo"/> #}
			<img class="nav-img" src="{{asset('uploads/images/Coeur_bleu_rose_lumineux_redim3_2.jpg')}}" alt="Combined double Hearts Logo"/>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active upnavlink text-light" aria-current="page" href="{{path('app_home')}}">Accueil</a>
				</li>
				{% if app.user %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle upnavlink text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Belles Rencontres
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item downlink" href="{{path('app_man_user')}}">Rencontrer des Hommes</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="{{path('app_woman_user')}}">Rencontrer des Femmes</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="{{path('app_user')}}">Tout voir</a>
						</li>
					</ul>
					{% else %}
					<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle upnavlink text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Belles Rencontres
					</a>
					
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item downlink" href="{{path('app_login')}}">Rencontrer des Hommes</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="{{path('app_login')}}">Rencontrer des Femmes</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="{{path('app_login')}}">Tout voir</a>
						</li>
					</ul>
					{% endif %}
				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle upnavlink text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Mon compte
					</a>
					<ul class="dropdown-menu">

						{% if app.user %}
						<li>
							<a class="dropdown-item downlink" href="{{path('app_profil')}}">Profil</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="{{ path('app_logout') }}">Deconnexion</a>
						</li>
							{% if is_granted('ROLE_ADMIN') %}
						<li>
							<a class="dropdown-item downlink" href="{{path('app_register')}}">Inscription</a>
						</li>
							{% endif %}
						{% else %}
						<li>
							<a class="dropdown-item downlink" href="{{path('app_register')}}">Inscription</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="{{ path('app_login') }}">Connexion</a>
						</li>
						{% endif %}
					</ul>
				</li>
				{% if app.user %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle upnavlink text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Paramètres
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item downlink" href="{{path('app_edit_user', {id: app.user.id})}}">Editer mon compte</a>
						</li>
						<li>
							<a class="dropdown-item downlink" href="#">Liens utiles</a>
						</li>

					</ul>
				</li>
				{% endif %}

				{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link active upnavlink text-light" aria-current="page" href="{{ path('app_dashboard')}}">Back-Office</a>
				</li>
				{% endif %}
				{% if app.user %}
				<li>
				{#<a href="{{ path('app_profil') }}" class="dropdown-item">{{
					app.user.firstname
					}}</a>
				</li>#}
            {% endif %}
				<li class="nav-item">
					<a class="nav-link active upnavlink text-light" aria-current="page" href="#">Contact</a>
				</li>

			</ul>
		</div>
	</div>
</nav>
